name: Build ZMK firmware

on:
  workflow_dispatch:
  push:
    paths:
      - "config/blecorne.conf"
      - "config/blecorne.keymap"
      - "config/west.yml"
      - "boards/**"
      - "build.yaml"

jobs:
  build:
    uses: zmkfirmware/zmk/.github/workflows/build-user-config.yml@v0.3.0
